name: Mirroring-st

on:
  push:
    branches: [develop]

jobs:
  to_codecommit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url:
            ssh://git-codecommit.ap-south-1.amazonaws.com/v1/repos/isp-codecommit-st
          ssh_private_key:
            ${{ secrets.ISP_CODECOMMIT_SSH_PRIVATE_KEY_ST }}
          ssh_username:
            ${{ secrets.ISP_CODECOMMIT_SSH_PRIVATE_KEY_ID_ST }}